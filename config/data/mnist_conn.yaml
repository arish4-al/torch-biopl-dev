# @package _global_
data:
  dataset: mnist
  root: data
  batch_size: 256
  num_workers: 8
  shuffle_test: true

model:
  rnn_kwargs:
    input_size: 784
    input_projection: connectivity/sunny/input_projection_matrix.pt
  num_classes: 10

optimizer:
  lr: 0.01
